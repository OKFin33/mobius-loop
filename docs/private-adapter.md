# Private Adapter Boundary

Keep deployment-specific material outside the public repo.

Examples:

- real machine paths;
- real sync remotes;
- real project names;
- personal role or persona files;
- background scheduler configuration;
- raw session records;
- workstate graphs;
- private knowledge bases.

The public repo should provide generic contracts and fixtures. Private users can
map those contracts to their own local runtime.

